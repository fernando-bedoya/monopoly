// URL previo quedaba para otro servicio; ahora centralizamos el backend de Monopoly.
export const API_BASE = "http://127.0.0.1:5000"; // Ajusta aquí si cambia el backend
export const LEGACY_DEV_SERVER = "http://localhost:3000"; // Referencia antigua (por si algo lo usa)